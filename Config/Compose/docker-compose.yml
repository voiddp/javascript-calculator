version: '3'
services:
   calc:
     image: "${build}"
     container_name: calc
     ports:
      - 3000:80
   proxy:
     build:
       context: .
       dockerfile: Proxy/Dockerfile
     container_name: proxy
     ports:
      - 80:80
     links:
      - calc